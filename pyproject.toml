[project]
name = "agentic-rag"
version = "0.1.0"
description = "Agentic RAG platform with Milvus, vLLM, and Multimodal capabilities."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.128.0",
    "pydantic>=2.12.5",
    "python-socketio>=5.16.0",
    "uvicorn>=0.40.0",
]
